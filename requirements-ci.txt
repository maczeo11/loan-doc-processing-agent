# FinScan AI - Fast CI Dependency Manifest
# Used by GitHub Actions to avoid compiling heavy ML/GPU wheels in CI runners

# Web & API Testing
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Database & Persistence
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
aiosqlite>=0.20.0
psycopg[binary]>=3.1.18
redis>=5.0.3

# Orchestration & Adapters
# rapidfuzz: required by core/rules/identity.py (RULE-ID-01), imported at module
# load via core/graph/nodes.py. Prebuilt wheel, no compilation in CI.
rapidfuzz>=3.6.2
langgraph>=1.0,<2.0
langchain-core>=0.1.38
# langchain-openai: lightweight (no torch/heavy wheel) - needed to test the
# feature-flagged agentic Q&A path (apps/api/agent.py) with ChatOpenAI mocked;
# tests must never hit a live Groq/OpenAI endpoint (AGENTS.md: "CI must never
# execute paid cloud API calls").
langchain-openai>=0.1.1
boto3>=1.34.0
pymupdf>=1.24.0
pyyaml>=6.0

# Retrieval (RAG) - prebuilt wheels / pure-python only; faiss + sentence-transformers
# stay optional (try/except in core/rag/indexer.py) to keep CI fast
numpy>=1.26.0
rank-bm25>=0.2.2

# Machine Learning & Document Classification
scikit-learn==1.8.0
joblib>=1.3.2

# Testing & Quality Gates
pytest>=8.1.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
fakeredis>=2.20.0
ruff>=0.3.0
