# FinScan AI Architecture & Component Boundaries

FinScan AI is a GenAI-assisted loan document verification and underwriting agent designed for bank underwriters.

## Architectural Principles
1. **Deterministic code decides. AI explains. A human approves.**
   - No financial arithmetic or pass/flag decision is generated by an LLM.
   - LLMs are used solely to explain, summarize, and retrieve contextual policy knowledge.
2. **Every fact carries evidence.**
   - Extracted facts require an `EvidenceRef` with page number and bounding-box coordinates.
   - Missing or unverifiable facts return `UNKNOWN`, never a guess.
3. **Ports and Adapters:**
   - Storage: Local Filesystem (dev) / S3 (cloud)
   - Queue: PostgreSQL `FOR UPDATE SKIP LOCKED` (dev) / SQS (cloud)
   - LLM: OpenCode Zen / Local Qwen3 GGUF (offline)

---

## Component Boundaries & Ownership

| Component | Directory | Owner | Responsibilities |
|---|---|---|---|
| **Contracts & CI** | `core/contracts/`, `tests/` | Manjunath | API contracts, generated client, integration, CI, release checklist, evaluation harness |
| **LangGraph & Worker** | `core/graph/`, `worker/`, `adapters/` | Bhanu Teja | Worker leases, pause/resume, recovery, SQS+DLQ/S3 layout, model serving. **Lead:** architecture, gate calls, viva |
| **Extraction & OCR** | `core/extraction/` | Jeevan | PDF text with coordinates, OCR routing, evidence extraction, page-span validation |
| **Rules & Synthetic Data** | `core/rules/`, `data/`, `scripts/` | Sravanthi | Synthetic dossier generator with ground truth, deterministic completeness/salary/tax/bank rules |
| **Classifier ML** | `ml/` | Karthik | Classifier training (baseline vs encoder) on local GPU, splits/eval, MLflow, release bundle |
| **FastAPI & Host Cloud** | `apps/api/`, `infra/` | Balaji | FastAPI, PostgreSQL/outbox. **Host cloud:** EC2, Caddy HTTPS, Compose, SQS/S3/IAM, rollback/stamping |
| **Reviewer SPA** | `apps/ui/` | Akshaya | Three-pane dashboard, evidence overlays, corrections, PDF/JSON export |
| **Hybrid RAG & QA** | `core/rag/`, `policies/`, `eval/` | Sai Mokshith | Hybrid RAG with citation checks, injection guardrails, the 40-question set, integrated QA |

---

## Pipeline Execution Flow
```
Upload Dossier 
   │
   ▼
OCR Routing (Native text -> PaddleOCR CPU -> Textract)
   │
   ▼
Classify Document Pages (TF-IDF / DistilBERT)
   │
   ▼
Extract Structured Facts with EvidenceRef
   │
   ▼
Deterministic Rules (Completeness, Salary Reconcile, Tax, KYC)
   │
   ▼
Hybrid RAG Retrieval (Policy + Application Corpus)
   │
   ▼
Explain & Summarize (OpenCode Zen / Qwen GGUF)
   │
   ▼
Grounding Validation (Strict Citation Check)
   │
   ▼
Interrupt & Reviewer Human Sign-Off (Approved / Flagged / Needs Info)
```
